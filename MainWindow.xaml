<Window x:Class="Captcha.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Captcha" Height="200" Width="350">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Image Grid.Row="0" x:Name="CaptchaImage" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" x:Name="UserCaptchaInput" Width="200" HorizontalAlignment="Center"
                 Height="20" VerticalAlignment="Center"/>
        <Button Grid.Row="2" x:Name="Submit" Click="Submit_Click" Content="Проверить"
                Margin="16"/>
    </Grid>
</Window>
